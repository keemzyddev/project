import { makeStyles } from '@mui/styles'

 const useStyles = makeStyles(() => ({
    mainContainer: {
      display: 'flex',
      alignItems: 'center',
    },
    smMargin: {
      margin: '5px',
    },
    actionDiv: {
      textAlign: 'center',
    },
  }));

  export default useStyles